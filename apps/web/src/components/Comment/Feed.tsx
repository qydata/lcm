import type {FC} from "react";

interface FeedProps {
    isHidden: boolean;
    publicationId: string;
}

const Feed: FC<FeedProps> = ({isHidden, publicationId}) => {

    return (
        <></>
    );
};

export default Feed;
